# apps/api

FastAPI backend app.

## Local setup

1. `python -m venv .venv`
2. Activate venv
3. `pip install -r requirements.txt`
4. Run with `uvicorn app.main:app --reload`

Keep all Python installs inside this app's `.venv`.
