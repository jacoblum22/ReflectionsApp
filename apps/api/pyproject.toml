[project]
name = "reflectionsapp-api"
version = "0.1.0"
description = "FastAPI backend for ReflectionsApp"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
